export type FormState = {
  jmeno: string;
  prijmeni: string;
  email: string;
  telefon: string;
};
export type PasswordForm = {
  currentPassword: string;
  newPassword: string;
  confirmPassword: string;
};
